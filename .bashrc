#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias sf='screenfetch'
alias gc='git clone'
alias ls='ls --color=auto'
alias x='startx'
PS1='\W >'

export VISUAL="nano"

